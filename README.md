dev notes:

let's leverge git for version control, and sharing model files/metrics/charts

for now, still using notebook. when/if we structure to a more sophisticated file system, we will keep the same code verbatim, and only deal with distributed imports over the files, and splitting cells into their own files.
